[package]
name = "zfaktury-testutil"
version = "0.1.0"
edition.workspace = true

[dependencies]
zfaktury-domain = { path = "../zfaktury-domain" }
chrono = { version = "0.4", default-features = false, features = ["std"] }
similar = "2"
